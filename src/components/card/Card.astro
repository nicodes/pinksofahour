---
import styles from "./card.module.scss";

interface Props {
  el?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { el = "section", class: className } = Astro.props;
const El = el;
---

<El class={`${styles.card} ${className}`}>
  <slot />
</El>
