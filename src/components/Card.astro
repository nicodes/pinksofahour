---
const { className } = Astro.props;
---

<section class={className}>
  <slot />
</section>

<style>
  section {
    padding: 24px;
    border-radius: 12px;
    background-color: rgba(0, 0, 0, 0.5);
  }
</style>
