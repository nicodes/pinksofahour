<section>
  <slot />
</section>

<style>
  section {
    padding: 24px;
    border-radius: 12px;
    background-color: rgba(0, 0, 0, 0.5);
    max-width: calc(
      560px - (2 * 24px)
    ); /* based on video width - (2 * padding) */
  }

  section a {
    color: white;
  }
</style>
